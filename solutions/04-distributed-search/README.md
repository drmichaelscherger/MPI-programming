# Lab 04 Reference Solution

Integer-division boundaries partition every index exactly once, including when there are more workers than elements. The manager computes the minimum valid result, so message arrival order cannot change the answer.
