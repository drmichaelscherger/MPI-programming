# Unit 04 Exercises

1. For rank `r` in a ring of size `p`, give formulas for predecessor and successor.
2. Trace the Unit 04 ring with four ranks. What token value returns to rank 0?
3. Compare pipeline latency and throughput. Why can throughput improve without reducing one-item latency?
4. Design tags and payloads for a dynamic manager/worker protocol with work, result, and termination messages.
5. A manager has two tasks and five workers. Describe a valid protocol that terminates all workers.
6. Identify one safety property and one liveness property for a distributed search.
7. Explain why collecting results with `MPI_ANY_SOURCE` does not imply that result order equals task order.
8. Challenge: revise the static worker-pool example on paper so each worker can process zero or more tasks.

Answer key: [`../../solutions/exercises/unit-04.md`](../../solutions/exercises/unit-04.md).
